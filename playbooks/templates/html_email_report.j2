<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>System Configuration Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .success { color: green; }
        .failure { color: red; }
        .no-change { color: orange; } /* Styling for no changes needed */
    </style>
</head>
<body>
    <h1>System Configuration Report : {{ new_hostname | upper }}</h1>
    <p>Date of Report: {{ ansible_date_time.iso8601 }}</p>
    <table>
        <tr>
            <th>Task</th>
            <th>Status</th>
        </tr>
        <tr>
            <td>Hostname Change</td>
            <td class="{{ 'success' if hostname_change_output.rc|default(0) == 0 else 'failure' }}">
                {{ 'Complete' if hostname_change_output.rc|default(0) == 0 else 'Failed' }}
            </td>
        </tr>
    </table>
    <br>
    <h2>Sudoers Update Report</h2>
    <table>
        <tr>
            <th>Entity</th>
            <th>Action</th>
            <th>Status</th>
        </tr>
        {% for result in sudo_groups_result.results %}
        <tr>
            <td>{{ result.item }}</td>
            <td>Add to sudoers (Group)</td>
            <td class="{{ 'success' if result.changed else 'no-change' }}">
                {{ 'Added' if result.changed else 'Already present' }}
            </td>
        </tr>
        {% endfor %}
        {% for result in sudo_users_result.results %}
        <tr>
            <td>{{ result.item }}</td>
            <td>Add to sudoers (User)</td>
            <td class="{{ 'success' if result.changed else 'no-change' }}">
                {{ 'Added' if result.changed else 'Already present' }}
            </td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>
