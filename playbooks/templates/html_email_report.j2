<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>System Configuration Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .success { color: green; }
        .failure { color: red; }
    </style>
</head>
<body>
    <h1>System Configuration Report for {{ new_hostname }}</h1>
    <p>Date of Report: {{ ansible_date_time.iso8601 }}</p>
    <table>
        <tr>
            <th>Task</th>
            <th>Status</th>
        </tr>
        <tr>
            <td>Hostname Change</td>
            <td class="{{ 'success' if hostname_change_output is not defined or (hostname_change_output.rc is defined and hostname_change_output.rc == 0) else 'failure' }}"> 
                {{ 'Already set' if hostname_change_output is not defined else ('Complete' if hostname_change_output.rc == 0 else 'Failed') }}
            </td>
        </tr>
        <!-- Add more rows as needed for other tasks -->
    </table>
</body>
</html>
