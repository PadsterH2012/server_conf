<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>System Configuration Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .changed { color: green; }
        .unchanged { color: orange; }
        .failed { color: red; }
    </style>
</head>
<body>
    <h1>System Configuration Report for {{ new_hostname }}</h1>
    <p>Date of Report: {{ ansible_date_time.iso8601 }}</p>
    <table>
        <tr>
            <th>Task</th>
            <th>Sub Task</th>
            <th>Status</th>
            <th>Details</th>
        </tr>
        <tr>
            <td>Hostname Change</td>
            <td>N/A</td>
            <td class="{{ 'changed' if hostname_change_output.rc|default(0) == 0 else 'failed' }}">
                {{ 'Complete' if hostname_change_output.rc|default(0) == 0 else 'Failed' }}
            </td>
            <td>N/A</td>
        </tr>
        <tr>
            <td rowspan="2">Kernel Limit Configuration</td>
            <td>DNF</td>
            <td class="unchanged">No Changes Needed</td>
            <td>Set to 2 in /etc/dnf/dnf.conf</td>
        </tr>
        <tr>
            <td>YUM</td>
            <td class="unchanged">No Changes Needed</td>
            <td>Set to 2 in /etc/yum.conf</td>
        </tr>
        <!-- Begin PAM configuration section -->
        <tr>
            <td rowspan="4">Update PAM Configurations</td>
            <td>/etc/pam.d/atd</td>
            <td class="failed">Failed</td>
            <td>Error: Destination does not exist!</td>
        </tr>
        <tr>
            <td>/etc/pam.d/crond</td>
            <td class="failed">Failed</td>
            <td>Change successful or no change needed.</td>
        </tr>
        <tr>
            <td>/etc/pam.d/password-auth</td>
            <td class="failed">Failed</td>
            <td>Change successful or no change needed.</td>
        </tr>
        <tr>
            <td>/etc/pam.d/system-auth</td>
            <td class="failed">Failed</td>
            <td>Change successful or no change needed.</td>
        </tr>
        <!-- Other tasks can be similarly structured -->
    </table>
</body>
</html>
